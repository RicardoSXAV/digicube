/**
 * Fabric-only mixins. Put a mixin here only when it touches Fabric API or a class
 * that does not exist on other loaders; everything else belongs in
 * {@code com.digicube.mixin} in the common module.
 */
package com.digicube.fabric.mixin;
