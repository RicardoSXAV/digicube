package com.digicube.entity;

/** Read-only, tracked presentation flag for living entities; effect duration remains server-owned. */
public interface IceMarkState {
    boolean digicube$hasIceMark();
}
